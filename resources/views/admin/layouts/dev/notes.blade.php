{{--

    --------------------------------------------------------
        {{ config('app.name') }}
        {{ config('app.url') }}
        {{config('mail.mailers.smtp.username')}}
        {{config('mail.mailers.contact.username')}}
    ------------------------------------------------------

--}}
